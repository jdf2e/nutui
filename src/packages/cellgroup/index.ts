import { NutComponent } from '../../common/component.ts'

NutComponent({
  props: {
    title: { type: String, default: '' },
    desc: { type: String, default: '' }

  },

  data() {
    return {};
  },
  methods: {
  }
});
